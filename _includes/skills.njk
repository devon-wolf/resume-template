<section>
    <h1>Technologies and Tools</h1>
    <ul class="skills">
        {%- for skill in collections.skills -%}
            <li class="two-col offset">
                <h3>{{ skill.data.category }}</h3>
                <span>
                    {{ skill.data.contents.join(', ') }}
                </span>
            </li>
        {%- endfor -%}
    </ul>
</section>