<section>
    <h1>{{ skillsTitle }}</h1>
    <ul class="skills">
        {%- for skill in skills -%}
            <li class="two-col offset">
                <h3>{{ skill.category }}</h3>
                <span>
                    {{ skill.contents.join(', ') }}
                </span>
            </li>
        {%- endfor -%}
    </ul>
</section>